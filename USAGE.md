# 快速使用指南

## 🚀 快速开始

### 1. 启动系统

#### 方式一：使用启动脚本（推荐）

**启动后端：**
```bash
./start_backend.sh
```

**启动前端：**（在新终端中）
```bash
./start_frontend.sh
```

#### 方式二：手动启动

**后端：**
```bash
# 安装依赖（首次运行）
pip3 install -r backend/requirements.txt

# 启动后端服务
python3 -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

**前端：**
```bash
cd frontend

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

### 2. 访问系统

- **前端界面**: http://localhost:3000
- **后端API**: http://localhost:8000
- **API文档**: http://localhost:8000/api/docs

## 📖 功能使用

### 数据概览 (Dashboard)

1. 访问首页即可查看数据概览
2. 使用日期范围选择器筛选时间段
3. 选择店铺查看特定店铺数据
4. 查看：
   - 采购总额、订单总数、产品种类
   - 店铺采购对比图
   - 支付状态分布饼图
   - TOP10 产品排行榜

### 订单管理

#### 查看订单列表
1. 点击左侧菜单 "订单管理"
2. 使用搜索框搜索订单号或产品名称
3. 使用筛选器：
   - 日期范围
   - 店铺
   - 支付状态

#### 创建新订单
1. 点击 "新建订单" 按钮
2. 填写订单信息：
   - 订单编号（必填，唯一）
   - 产品名称（必填）
   - 采购金额（必填）
   - 订单日期
   - 店铺名称（必填）
   - 支付状态
3. 点击 "确定" 保存

#### 编辑订单
1. 在订单列表中点击 "编辑"
2. 修改订单信息
3. 点击 "确定" 保存

#### 删除订单
1. 在订单列表中点击 "删除"
2. 确认删除操作

### 产品分析

1. 点击左侧菜单 "产品分析"
2. 查看产品采购统计：
   - 产品采购金额排行图
   - 详细的产品统计表格
   - 每个产品的采购总额、订单数、平均金额、占比
3. 使用筛选器：
   - 日期范围
   - 店铺
   - TOP数量（20/50/100/200）

### 数据导入导出

#### 导入Excel数据
1. 点击左侧菜单 "数据导入导出"
2. 在导入区域：
   - 点击上传区域或拖拽Excel文件
   - 支持 .xlsx 和 .xls 格式
   - 文件大小不超过 10MB
3. 等待上传和处理
4. 查看导入结果统计

#### 导出Excel数据
1. 在导出区域选择筛选条件（可选）：
   - 日期范围
   - 店铺
2. 点击 "导出 Excel 数据"
3. Excel文件将自动下载

## 💡 使用技巧

### 1. 快速筛选
- 在订单列表中使用组合筛选可以快速定位订单
- 例如：选择特定日期范围 + 特定店铺 + 特定支付状态

### 2. 批量操作
- 使用Excel导入功能可以批量添加或更新订单
- 导出功能可以快速生成报表

### 3. 数据分析
- Dashboard提供了直观的数据可视化
- 产品分析页面可以深入了解各产品的采购情况
- 使用日期筛选可以对比不同时期的数据

### 4. API使用
- 访问 http://localhost:8000/api/docs 查看完整的API文档
- 可以直接在Swagger UI中测试API
- 适合开发自定义集成或自动化脚本

## 🔧 常见问题

### Q: 后端启动失败？
**A:** 检查：
1. Python版本是否 >= 3.8
2. 是否安装了所有依赖: `pip3 install -r backend/requirements.txt`
3. 端口8000是否被占用

### Q: 前端启动失败？
**A:** 检查：
1. Node.js版本是否 >= 16
2. 是否安装了依赖: `cd frontend && npm install`
3. 端口3000是否被占用

### Q: 前端无法连接后端？
**A:** 确保：
1. 后端正在运行在 http://localhost:8000
2. 前端代理配置正确（vite.config.ts）
3. 浏览器控制台没有CORS错误

### Q: 导入Excel失败？
**A:** 检查：
1. Excel格式是否正确
2. 文件大小是否超过10MB
3. Excel中的数据格式是否符合要求
4. 查看后端日志了解详细错误

### Q: 数据库在哪里？
**A:** SQLite数据库文件位于 `backend/procurement.db`

## 📞 获取帮助

如遇到问题：
1. 查看后端日志（终端输出）
2. 查看浏览器控制台错误
3. 访问API文档: http://localhost:8000/api/docs
4. 查看 README.md 了解更多信息

## 🎯 最佳实践

1. **定期备份数据**: 使用导出功能定期导出数据
2. **使用筛选器**: 处理大量数据时使用筛选器提高效率
3. **验证数据**: 导入前先检查Excel数据格式
4. **监控统计**: 定期查看Dashboard了解采购趋势
5. **API集成**: 对于自动化需求，使用API接口

---

祝您使用愉快！ 🎉
