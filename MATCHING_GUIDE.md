# 1688订单产品名称匹配测试指南

## 📋 问题背景

在导入1688订单数据时，面临一个关键问题：

- **1688订单**：产品名称是详细的商品描述（如"管道疏通剂袋装 25g 疏通剂厕所马桶通渠粉 马桶管道清洁剂厂家"）
- **采购系统**：产品名称是简化的（如"管道疏通剂"）

需要一个智能匹配工具来测试和验证两者的匹配效果。

## 🎯 测试工具功能

`api/test_product_matching.py` 提供以下功能：

### 1. 智能匹配算法

实现了三层匹配策略：

#### 🔹 精确匹配（优先级最高）
系统产品名完整出现在1688标题中
```
1688: "管道疏通剂袋装 25g..."
系统: "管道疏通剂"
结果: ✓ 精确匹配 (100%置信度)
```

#### 🔹 关键词匹配
提取并匹配关键词
```
1688: "强力补鞋胶专用鞋胶..."
系统: "白色英文鞋胶"
关键词: "鞋胶"
结果: ✓ 关键词匹配 (50%置信度)
```

#### 🔹 模糊匹配
基于字符串相似度计算
```
相似度阈值: 60%
用于处理拼写相近的产品名
```

### 2. 两种测试场景

#### 场景1：使用全部产品作为候选
```python
use_filtered_products=False
```
适用于：1688订单的产品可能对应系统中**任意月份**出现的产品

#### 场景2：只使用指定月份的产品作为候选
```python
use_filtered_products=True
```
适用于：假设1688订单的产品都是该月份采购的，缩小匹配范围

## 🚀 使用方法

### 快速开始

```bash
cd api
python3 test_product_matching.py
```

### 测试结果

工具会生成：

1. **控制台输出**：详细的匹配统计和示例
2. **Excel文件**：完整的匹配结果

#### 生成的文件

| 文件名 | 说明 |
|--------|------|
| `matching_results_all_products.xlsx` | 使用全部产品匹配的结果 |
| `matching_results_oct_only.xlsx` | 只使用10月产品匹配的结果 |

#### Excel文件内容

| 列名 | 说明 |
|------|------|
| 订单编号 | 1688订单号 |
| 1688标题 | 完整的产品标题 |
| 金额 | 实付款 |
| 是否匹配 | 是/否 |
| 匹配产品 | 系统中的产品名 |
| 匹配方法 | exact/keyword/fuzzy/none |
| 置信度 | 匹配可信度 (0-100%) |
| 匹配关键词 | 命中的关键词 |

## 📊 实际测试结果（2025年10月数据）

### 场景1：使用全部113种产品

```
✅ 匹配成功: 38/65 (58.5%)
  - 精确匹配: 35 条 (53.8%)
  - 关键词匹配: 3 条 (4.6%)
  - 模糊匹配: 0 条 (0.0%)
❌ 未匹配: 27 条 (41.5%)

平均匹配置信度: 96%
```

### 场景2：只使用10月的16种产品

```
✅ 匹配成功: 33/65 (50.8%)
  - 精确匹配: 31 条 (47.7%)
  - 关键词匹配: 2 条 (3.1%)
  - 模糊匹配: 0 条 (0.0%)
❌ 未匹配: 32 条 (49.2%)

平均匹配置信度: 97%
```

### 匹配成功示例

```
✓ 管道疏通剂袋装 25g...
  → 管道疏通剂 (精确匹配, 100%)

✓ 补鞋专用胶水补鞋胶防水粘鞋匠...
  → 白色英文鞋胶 (精确匹配, 100%)

✓ 强力补鞋胶专用鞋胶修鞋匠...
  → 白色英文鞋胶 (关键词匹配, 50%, 关键词: 鞋胶)
```

### 未匹配示例

```
✗ 高品质AB胶铸工胶批发粘金属...
  原因: "铸工胶" vs "高铸胶" - 关键词顺序不同

✗ 厂家供货 布莱恩b7000强力针头胶水...
  原因: "b7000" vs "B-7000" - 大小写和连字符

✗ 243螺丝胶水243厌氧胶螺纹锁固剂...
  原因: "243" 太短，被过滤为噪音词
```

## 🔧 自定义测试

### 修改测试月份

编辑 `test_product_matching.py`:

```python
# 测试2025年11月的数据
results = tester.test_matching(year=2025, month=11, use_filtered_products=False)
```

### 优化匹配规则

#### 1. 调整噪音词

```python
noise_words = ['袋装', '瓶装', '盒装', ...]
```

#### 2. 调整置信度阈值

```python
# 关键词匹配阈值（当前50%）
if best_keyword_score >= 0.5:

# 模糊匹配阈值（当前60%）
if similarity >= 0.6:
```

#### 3. 添加自定义映射规则

对于已知的难以匹配的产品，可以添加手动映射：

```python
custom_mappings = {
    '铸工胶': '高铸胶',
    'b7000': 'B-7000',
    '243螺丝胶': '243厌氧剂',
}
```

## 💡 提升匹配率的建议

### 短期方案：手动映射表

基于测试结果，创建一个产品映射表：

```python
# api/product_mapping.py
PRODUCT_MAPPINGS = {
    # 1688关键词 : 系统产品名
    '铸工胶': '高铸胶',
    '高铸胶': '高铸胶',
    '铸胶': '高铸胶',

    'b7000': 'B-7000',
    'b-7000': 'B-7000',

    '243': '243厌氧剂',
    '243螺丝胶': '243厌氧剂',
    '厌氧胶': '243厌氧剂',

    'opp': 'OPP袋',
    'opp袋': 'OPP袋',
}
```

### 长期方案：机器学习

1. 收集人工标注的匹配数据
2. 训练文本相似度模型
3. 实现智能推荐（显示Top 3候选产品）

## 📝 使用场景

### 1. 批量导入前的预检查

在正式导入1688订单前，先运行测试：

```bash
python3 test_product_matching.py
# 查看匹配率和未匹配项
# 决定是否需要手动处理未匹配订单
```

### 2. 优化产品名称

根据测试结果，统一系统中的产品命名：

```
不好: "高铸胶", "铸工胶", "铸胶" (分散)
更好: 统一为 "高铸胶"，其他作为别名
```

### 3. 验证新的匹配算法

修改匹配逻辑后，重新测试验证效果：

```bash
# 修改代码
vim test_product_matching.py

# 重新测试
python3 test_product_matching.py

# 对比匹配率变化
```

## 🎓 匹配算法原理

### 关键词提取

```python
输入: "白色英文鞋胶"
移除噪音词: 白色, 英文
关键词: ["鞋胶", "白色英文鞋胶"]
```

### 匹配计算

```python
1688标题: "强力补鞋胶专用鞋胶修鞋匠粘鞋胶..."
系统产品: "白色英文鞋胶"
关键词: ["鞋胶", "白色英文鞋胶"]

匹配情况:
  ✓ "鞋胶" 在标题中
  ✗ "白色英文鞋胶" 不在标题中

置信度 = 1/2 = 50%
```

## 🐛 常见问题

### Q1: 为什么"铸工胶"没有匹配到"高铸胶"？

A: 精确匹配要求系统产品名**完整出现**在1688标题中。"铸工胶"和"高铸胶"是不同的词。

**解决方案**：
1. 添加到手动映射表
2. 在系统中使用"铸工胶"作为别名

### Q2: 为什么场景2的匹配率更低？

A: 场景2只使用10月份出现的16种产品作为候选，而1688订单可能包含其他月份采购的产品。

**建议**：对于首次导入，使用场景1更合理。

### Q3: 如何处理未匹配的订单？

A: 三种方式：
1. 在导入前手动添加产品映射
2. 导入时提示用户选择产品
3. 先以"未分类"导入，后续人工标注

## 📚 相关文档

- [IMPORT_GUIDE.md](IMPORT_GUIDE.md) - 数据导入指南
- [OPTIMIZATION_PLAN.md](OPTIMIZATION_PLAN.md) - 系统优化计划（包含供应商管理和产品映射改进）

## 🔗 下一步

基于测试结果，可以：

1. ✅ 创建产品映射表提升匹配率
2. ✅ 实现1688订单导入向导（带产品选择）
3. ✅ 优化系统产品命名规范
4. ✅ 添加产品别名/同义词功能

---

*创建时间: 2025-11-02*
*测试数据: 2025年10月1688订单（65条）*
